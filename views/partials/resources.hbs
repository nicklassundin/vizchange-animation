
{{#each files}}
    <!--
        <image class="image-block {{this.level}}" id={{this.id}} src={{this.file}}></image>
    -->
    <image class="image-block {{this.level}}" style="z-index: {{this.level}}" id={{this.id}} src={{this.file}}></image>
{{/each}}